import { Meta, Typeset } from '@storybook/blocks';

<Meta title="Design System/Typography" />

export const typography = {
  type: {
    primary: 'Pretendard'
  },
  weight: {
    regular: '400',
    medium: '500',
    semibold: '600',
    bold: '700',
  },
  size: {
    "text-xs": 12,
    "text-sm": 13,
    "text-md": 14,
    "text-lg": 16,
    "text-2lg": 18,
    "text-xl": 20,
    "text-2xl": 24,
    "text-3xl": 32,
  },
  lineHeight: {
    "text-xs": 20,
    "text-sm": 22,
    "text-md": 24,
    "text-lg": 26,
    "text-2lg": 26,
    "text-xl": 32,
    "text-2xl": 32,
    "text-3xl": 42,
  },
};
 
export const SampleText = '에피그램, Pretendard';
 
# Typography
 
**Font Family:** Pretendard

**Weights:** 400(regular), 500(medium), 600(semibold), 700(bold)

## Font Sizes

<Typeset
  fontSizes={[
    Number(typography.size["text-xs"]),
    Number(typography.size["text-sm"]),
    Number(typography.size["text-md"]),
    Number(typography.size["text-lg"]),
    Number(typography.size["text-2lg"]),
    Number(typography.size["text-xl"]),
    Number(typography.size["text-2xl"]),
    Number(typography.size["text-3xl"]),
  ]}
  fontWeight={typography.weight.regular}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>

## Font Weights

<Typeset
  fontSizes={[Number(typography.size["text-lg"])]}
  fontWeight={typography.weight.regular}
  sampleText="Regular 400"
  fontFamily={typography.type.primary}
/>

<Typeset
  fontSizes={[Number(typography.size["text-lg"])]}
  fontWeight={typography.weight.medium}
  sampleText="Medium 500"
  fontFamily={typography.type.primary}
/>

<Typeset
  fontSizes={[Number(typography.size["text-lg"])]}
  fontWeight={typography.weight.semibold}
  sampleText="Semibold 600"
  fontFamily={typography.type.primary}
/>

<Typeset
  fontSizes={[Number(typography.size["text-lg"])]}
  fontWeight={typography.weight.bold}
  sampleText="Bold 700"
  fontFamily={typography.type.primary}
/>

